# posts

One file per scheduled run: `YYYY-MM-DD-morning.md` or `YYYY-MM-DD-evening.md` (IST date).

Each file is the run's full output: the posts, their suggested posting times, and the
ready-to-paste first replies with source links. The routine reads the most recent files
here before searching, so stories already sent don't get sent again.
